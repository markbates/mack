require 'pathname'
require Pathname(__FILE__).dirname.expand_path.parent.parent + 'spec_helper'

describe "alert" do
  
  it "should be settable in a controller"
  
  it "should be readable in a view"
  
  it "should persist until the next successful action"
  
end